{
  "name": "bb-webpack-frontend",
  "version": "0.0.1",
  "description": "bb webpack configuration",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "npm run clear && npm install",
    "build": "webpack --mode production --colors --progress",
    "clear": "rimraf dist",
    "start": "npm run clear && webpack-dev-server --mode development --hot --colors --progress --open"
  },
  "author": "blindlybright",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.11"
  },
  "devDependencies": {
    "html-webpack-plugin": "^3.2.0",
    "mini-css-extract-plugin": "^0.7.0",
    "path": "^0.12.7",
    "uglifyjs-webpack-plugin": "^2.1.3",
    "webpack": "^4.35.0",
    "webpack-cli": "^3.3.5",
    "webpack-dev-server": "^3.7.2"
  }
}
